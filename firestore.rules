rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for authenticated users (temporary for development)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}